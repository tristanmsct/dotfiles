#compdef networkpwd

_networkpwd() {
    local -a ssids
    ssids=(${(f)"$(nmcli -t -f NAME,TYPE connection show | awk -F: '$2=="802-11-wireless"{print $1}')"})
    _describe 'ssid' ssids
}

_networkpwd
