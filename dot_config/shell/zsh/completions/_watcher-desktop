#compdef watcher-desktop

_watcher-desktop() {
    local -a options
    options=(
        ':Check for changes in the .desktop files.'
        'update:Update the baseline file'
    )

    _describe 'watcher-desktop commands' options
}

_watcher-desktop "$@"
