#compdef watcher-pacnotes

_watcher-pacnotes() {
    local -a options
    options=(
      'add:Add a package and its reason'
      'list:List all or a specific package'
      'remove:Remove a package'
      'search:Search for a package or a reason'
    )

    _describe 'watcher-pacnotes commands' options
}

_watcher-pacnotes "$@"
