---
# - name: Configure Git
#   community.general.git_config:
#     name: "{{ item.name }}"
#     value: "{{ item.value }}"
#     scope: global
#   loop:
#     - { name: 'user.name', value: "{{ git_user_name }}" }
#     - { name: 'user.email', value: "{{ git_user_email }}" }

- debug:
    msg: "Installing Git {{ username }}"
