#!/usr/bin/env bash
if [ $# -eq 0 ]; then
    target="$HOME"
else
    target=$(zoxide query "$@" 2>/dev/null)
    if [ -z "$target" ]; then
        target="$HOME"
    fi
fi
kitty --app-id yazi -e yazi "$target" > /dev/null 2>&1 & disown
